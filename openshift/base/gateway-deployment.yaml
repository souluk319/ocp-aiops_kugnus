apiVersion: apps/v1
kind: Deployment
metadata:
  name: komsco-ai-gateway
  labels:
    app: komsco-ai-gateway
spec:
  replicas: 1
  selector:
    matchLabels:
      app: komsco-ai-gateway
  template:
    metadata:
      labels:
        app: komsco-ai-gateway
    spec:
      serviceAccountName: komsco-ai-gateway
      containers:
        - name: gateway
          image: komsco-ai-gateway:dev
          imagePullPolicy: Always
          ports:
            - name: https
              containerPort: 8443
          env:
            - name: OLS_BASE_URL
              value: "https://lightspeed-app-server.openshift-lightspeed.svc:8443"
            - name: OLS_CA_FILE
              value: "/var/run/configmaps/service-ca/service-ca.crt"
            - name: KOMSCO_AI_SECURITY_PHASE
              value: "phase0-1"
            - name: KOMSCO_AI_ENABLE_MUTATIONS
              value: "false"
            - name: KOMSCO_AI_ACTION_EXECUTOR_URL
              value: "http://komsco-ai-action-executor:8080"
            - name: KOMSCO_AI_DIAGNOSTICS_ENABLED
              value: "true"
            - name: KOMSCO_AI_HOST_DIAGNOSTICS_CONTROLLER_URL
              value: "http://komsco-ai-host-diagnostics-controller:8080"
            - name: KOMSCO_AI_RECORD_STORE_ENABLED
              value: "true"
            - name: KOMSCO_AI_RECORD_STORE_CONFIGMAP
              value: "komsco-ai-gateway-ledger"
            - name: KOMSCO_AI_PRODUCT_ACCESS_REVIEW_ENABLED
              value: "true"
            - name: KOMSCO_AI_PRODUCT_ACCESS_REVIEW_REQUIRED
              value: "false"
            - name: KOMSCO_AI_PRODUCT_ACCESS_REVIEW_GROUP
              value: "console.openshift.io"
            - name: KOMSCO_AI_PRODUCT_ACCESS_REVIEW_RESOURCE
              value: "consoleplugins"
            - name: KOMSCO_AI_PRODUCT_ACCESS_REVIEW_VERB
              value: "get"
            - name: KOMSCO_AI_PRODUCT_ACCESS_REVIEW_NAME
              value: "komsco-ai-console-plugin"
          readinessProbe:
            httpGet:
              path: /healthz
              port: https
              scheme: HTTPS
            initialDelaySeconds: 5
            periodSeconds: 10
          livenessProbe:
            httpGet:
              path: /healthz
              port: https
              scheme: HTTPS
            initialDelaySeconds: 15
            periodSeconds: 20
          securityContext:
            allowPrivilegeEscalation: false
            capabilities:
              drop:
                - ALL
            runAsNonRoot: true
          volumeMounts:
            - name: tls
              mountPath: /var/run/secrets/tls
              readOnly: true
            - name: service-ca
              mountPath: /var/run/configmaps/service-ca
              readOnly: true
      securityContext:
        seccompProfile:
          type: RuntimeDefault
      volumes:
        - name: tls
          secret:
            secretName: komsco-ai-gateway-tls
        - name: service-ca
          configMap:
            name: komsco-ai-service-ca
